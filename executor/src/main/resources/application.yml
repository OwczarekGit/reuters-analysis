spring:
  application:
    name: classification-executor
  datasource:
    username: user
    password: password
    url: jdbc:postgresql://localhost:5432/classification-data
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format-sql: true
      show-sql: true

config:
  thread-count: 8
  working-directory-path: "/home/kacper/Documents/reuters-analysis/classifier"
  classifier-path: "/home/kacper/Documents/reuters-analysis/classifier/target/release/analiza-danych"
  data-folder: "/home/kacper/Documents/reuters-analysis/classifier/reuters"
